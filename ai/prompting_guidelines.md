# AI Prompting Guidelines

When using AI tools:

1. Provide full architectural context before generating code.
2. Ask for small isolated components rather than entire systems.
3. Verify generated code against project rules before integration.
4. Reject code that mixes concerns (routes + logic + database).
5. Ensure generated code includes error handling and validation.

AI was used as a productivity tool, not as a replacement for architectural decision making.